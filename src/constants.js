export const PORT = process.env.PORT || 8000;

export const TIMESTAMP_TOLERANCE_SECONDS = Number(process.env.TIMESTAMP_TOLERANCE_SECONDS) || 300;

export const QUANTITY_TOLERANCE_PCT = Number(process.env.QUANTITY_TOLERANCE_PCT) || 0.01;
